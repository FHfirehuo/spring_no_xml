spring 的事件（）为bean与bean之间的消息通信提供了支持。
当一个bean处理完一个任务之后，希望另一个bean知道并能做响应的处理，只是我们就需要让另一个bean监听当前bean所发送的事件。
spring的事件需要遵循如下流程：
(1)自定义事件，集成applicationEvent
(2)定义事件监听器，实现applicationListener
(3)使用容器发布事件